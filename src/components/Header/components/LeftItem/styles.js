import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
  },
  text: {
    color: '#fff',
    fontSize: 18
  },
  touch: {
    flexDirection: 'row',
    alignItems: 'center',
  }
});
